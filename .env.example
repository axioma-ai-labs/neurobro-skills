# Neurobro Skills - environment template.
#
# Copy this file to .env and fill in your key:
#     cp .env.example .env
#
# .env is git-ignored, so your real key is never committed.

# NeuroAPI key. Create one in the dashboard under API Keys > Create key.
# Keys look like: neuro_<random>
NEUROAPI_API_KEY=
